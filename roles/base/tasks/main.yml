---
# base role — applied to ALL machines.
# Goal: install useful everyday packages (apt) + sane instance configuration.
# Constraints: use the `apt` module (no command/shell), no useless/heavy packages,
#              must be idempotent.
# See GitHub issue: "base role (all machines)".

# TODO: install useful packages (e.g. vim, curl, git, htop, ca-certificates ...)
# TODO: configure instance (locale, timezone, apt cache update ...)
